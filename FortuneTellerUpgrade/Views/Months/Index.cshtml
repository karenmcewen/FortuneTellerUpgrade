@model IEnumerable<FortuneTellerUpgrade.Models.Month>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Month1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Month1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MonthID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MonthID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MonthID })
        </td>
    </tr>
}

</table>
